# marathon-core

Type-only SDK and API specs for the Marathon platform. Published as `@marathon/core`.

## Building

Run `nx build marathon-core` to build the library.

## Running unit tests

Run `nx test marathon-core` to execute the unit tests via [Jest](https://jestjs.io).
